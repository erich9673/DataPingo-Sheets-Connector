services:
  - type: web
    name: datapingo-sheets-connector
    env: node
    plan: starter
    runtime: node
    buildCommand: npm ci && cd sheets-connector-backend && npm ci
    startCommand: node server.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
